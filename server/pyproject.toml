[project]
name = "android-streamdeck-server"
version = "0.1.0"
description = "Minimal local FastAPI server for Android Stream Deck"
requires-python = ">=3.11,<3.15"
dependencies = [
    "fastapi>=0.116,<1.0",
    "uvicorn[standard]>=0.35,<1.0",
]

[project.scripts]
streamdeck-server = "app.runner:main"

[dependency-groups]
dev = [
    "httpx>=0.28,<1.0",
    "jsonschema>=4.23,<5.0",
    "pytest>=8.4,<9.0",
    "ruff>=0.12,<1.0",
]

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
