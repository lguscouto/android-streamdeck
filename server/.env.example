# Example only: this file is not loaded automatically by the server.
# Export these variables in the shell before running `uv run streamdeck-server`.
# Do not place secrets in this file.
STREAMDECK_HOST=127.0.0.1
STREAMDECK_PORT=8765
